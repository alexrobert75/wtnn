<nav>
	<a class='Logo' href={{path('app_home')}}>WTNN</a>

	<ul class='list-nav'>
		<li>
			<a href={{path('app_prod')}}>{{'Sneakers' | upper}}</a>
		</li>
		<li>
			<a href={{path('app_brands')}}>{{'Brands' | upper}}</a>
		</li>

		{% if app.user %}
			<li>
				<a href={{path('app_account')}}>{{'Profile' | upper}}</a>
			</li>
			<li>
				<a href={{path('app_logout')}}>{{'Logout' | upper}}</a>
			</li>
		{% else %}
			<li>
				<a href={{path('app_register')}}>{{'Sign up' | upper}}</a>
			</li>
			<li>
				<a href={{path('app_login')}}>{{'Sign in' | upper}}</a>
			</li>
		{% endif %}
		<li>
	<a href={{path('app_contact')}}>{{'Contact' | upper}}</a>
		</li>
		<li>
			<a href={{path('cart_render')}}>{{'cart' | upper}}({{app.session.get('cart') | length}})</a>
		</li>
		{% if app.user.roles is defined and "ROLE_ADMIN" in app.user.roles %}
			<li>
				<a href={{path('admin')}}>
					{{'admin' | upper}}</a>
			</li>

		{% endif %}
	</ul>
</nav>
